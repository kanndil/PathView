# array of all the cells that can be drawn 
# contains:
    # cell names 
    # cell schemetics in HTML/SVG 
    # cell boolean expressions
    
    
# should be able to load the cell representation from a directory conating all the SVG representation


# should be able yto load the Lib/V file of the new library and extract the boolean representations of each cell


# match the cells and their boolean representation to the reference cells 
    
# build an new SVG file holding these pieces of information
